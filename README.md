# Dataverse Copilot Studio Agent App

This branch is a standalone app for building a Dataverse-backed Copilot Studio agent with strong follow-up handling and strict accuracy controls.

Main implementation assets are under:

- `module/dataverse-copilot-studio-agent/`
